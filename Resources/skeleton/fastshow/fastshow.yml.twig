{{ entityClass }}:
    Columns:
        {% for column in columns %}{{ column.getName() }}:
            label: {{ column.getName() }}
            show: true
            groups: ["default"]
        {% endfor %}
